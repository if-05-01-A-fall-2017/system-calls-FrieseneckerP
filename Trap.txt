A Trap is basically a procedure call that synchronously transfer the control.
It is  a software interrupt and is generated by the user program or by the error when the operating system is needed by it. 
So as to perform the system calls or an operation.
A trap instruction mainly switches from user mode to the kernel mode.